# What this changes

<!-- What behaviour is different afterwards, in a sentence or two. -->

## Why

<!-- The constraint or defect that forced it. If this works around something
    Plane's API does, say which endpoint and add a row to docs/COVERAGE.md. -->

## How it was checked

<!-- Which test covers it. "Ran it on my phone" has twice missed real defects
    here, so name the test if there is one, and say plainly if there is not. -->

- [ ] `flutter analyze --fatal-infos` is clean
- [ ] `flutter test` is green
- [ ] `dart format` leaves nothing to change
- [ ] `docs/COVERAGE.md` updated, or not affected
