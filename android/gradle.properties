org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
# Cannot be flipped to true yet, though every plugin here is now ready for it.
# FlutterPluginUtils.detectApplyingKotlinGradlePlugin walks the subprojects and
# applies kotlin-android to any that declare AGP without KGP — which is exactly
# what a migrated plugin looks like — and never reads this property. AGP 9 then
# rejects the plugin it was just handed. Revisit when flutter_tools honours it.
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
